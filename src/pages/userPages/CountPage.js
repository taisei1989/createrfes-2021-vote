/**
 * やりたいこと
 * - 指定した時間に合わせカウントダウンがスタートする
 * - カウントダウンページに移るとカウントが始まる
 * - 
 */

const CountPage = (phase) => {
    return (
      <div>
        <h1>CountPage</h1>
      </div>
    );
};

export default CountPage;
